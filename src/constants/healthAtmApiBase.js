/**
 * HealthATM / camp REST API origin (no trailing slash).
 * Use for all requests under this host, e.g. `${HEALTH_ATM_API_BASE_URL}/v1/camp/...`.
 */
export const HEALTH_ATM_API_BASE_URL = "https://system.healthatm.com/api";
